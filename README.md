# dsmlx Documentation

This is the documentation website for the [dubbo-kubernetes](https://github.com/apache/dubbo-kubernetes) project, built with MkDocs and a local theme (`theme/`).

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
.venv/bin/python -m mkdocs serve
```
